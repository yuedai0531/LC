OOP observations
you can't create a public class that does not have the same name as the Java file.
but you can create "default" classes

System.out.println();
does not print anything when inside of a constructor
